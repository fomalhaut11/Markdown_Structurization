# 项目架构全景

## 技术栈
| 层级 | 技术 | 版本 |
|------|------|------|
| 语言 | [例：TypeScript] | [例：5.x] |
| 前端 | [例：React] | [例：18.x] |
| 后端 | [例：Node.js + Express] | [例：20.x] |
| 数据库 | [例：PostgreSQL] | [例：16] |
| ORM | [例：Prisma] | [例：5.x] |
| 测试 | [例：Vitest + Playwright] | |

## 目录结构
```
[项目根目录]/
├── src/
│   ├── [模块A]/          # [模块A职责简述]
│   ├── [模块B]/          # [模块B职责简述]
│   └── shared/           # 共享工具/类型
├── tests/
├── docs/
└── [其他顶层目录]/
```

## 模块列表

| 模块 | 路径 | 职责 | 接口文档 |
|------|------|------|----------|
| [模块A] | src/[模块A]/ | [一句话描述] | docs/modules/[模块A].md |
| [模块B] | src/[模块B]/ | [一句话描述] | docs/modules/[模块B].md |

## 模块依赖方向
```
[模块A] → [模块B] → [模块C]
              ↓
          [模块D]
```
> 依赖方向规则：[例：上层可调用下层，反向禁止；shared 被所有模块依赖]

## 通信模式
| 场景 | 模式 | 说明 |
|------|------|------|
| [例：前后端通信] | [例：REST API] | [例：统一走 /api/v1 前缀] |
| [例：模块间事件] | [例：EventEmitter] | [例：通过 shared/events.ts 定义事件类型] |
| [例：异步任务] | [例：消息队列] | [例：Redis pub/sub] |

## 关键约定
- [例：所有 API 返回格式统一为 { code, data, message }]
- [例：错误处理统一使用 AppError 类]
- [例：环境变量通过 src/shared/config.ts 统一读取]
