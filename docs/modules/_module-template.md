# [模块名称]

## 职责
[一段话描述该模块的核心职责和边界，明确它做什么、不做什么]

## 公开接口

### `[函数/方法名称]`
- **签名**: `[完整函数签名，含参数类型和返回类型]`
- **功能**: [一句话描述]
- **参数**:
  - `[参数名]`: [类型] — [说明]
- **返回**: [返回值说明]
- **副作用**: [无 / 写数据库 / 发事件 / 调外部API / ...]
- **异常**: [可能抛出的错误类型和条件]

### `[下一个接口...]`
...

## 依赖关系
| 方向 | 模块/服务 | 用途 |
|------|-----------|------|
| 依赖 → | [例：shared/db] | 数据库连接 |
| 被依赖 ← | [例：api/routes] | 提供业务逻辑 |

## 内部结构（可选）
```
[模块名]/
├── index.ts        # 公开接口导出
├── service.ts      # 核心业务逻辑
├── repository.ts   # 数据访问层
└── types.ts        # 模块内部类型
```

## 注意事项
- [例：该模块的 createOrder 必须在事务中调用]
- [例：缓存 TTL 为 5 分钟，修改数据后需手动失效]
- [例：并发场景下需注意乐观锁版本号]
